## SECTION=hosttools_noprompt

config HOST_PYTHON
	tristate
	select HOST_OPENSSL
    select HOST_ZLIB if QT5
	default y if ALLYES
	help
	  Python is an interpreted, interactive,
	  object-oriented, extensible programming language.

